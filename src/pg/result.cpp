#include "pg/result.hpp"

// Implementation is fully inline in the header.
// This TU exists for future out-of-line definitions and to
// anchor the vtable/typeinfo if we ever add virtual methods.

namespace getgresql::pg {
} // namespace getgresql::pg
